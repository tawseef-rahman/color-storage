# ColorStorage

A web application that allows users to save color palettes and store colors within those palettes. Users can input colors using either HEX or RGB color codes and view their saved palettes.

## Features

- Add new color palettes.
- Add colors to a selected palette using HEX or RGB codes.
- View a single selected palette or all saved palettes.
